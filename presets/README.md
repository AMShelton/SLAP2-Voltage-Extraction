# Presets

Voltage-specific extraction presets will be added in Step 2 after the parameter contract is defined. At minimum, presets should make indicator polarity, F0 method/window, extraction channel, precision, and parallelism explicit.
